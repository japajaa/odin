extends layout

block content
  h1= title

  ul
    each dish in dish_list
      li
        a(href=dish.url) #{dish.name}
        |  (#{dish.category.map(cat => cat.name)})

    else
      li Ei yhtään ruokalajia.
